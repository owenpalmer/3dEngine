defaultCube = cube;
suzane = monke;

// drawObject(defaultCube, 0, 1);

defaultScene = {
    objects: [
        suzane,
        // defaultCube,
        // blob,
    ],
    navTransforms: {
        orbitX: 0,
        orbitY: 70,
        orbitZ: 45,
        panX: 0,
        panY: 0,
        zoom: 1,
    }
}