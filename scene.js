defaultCube = cube;
suzane = monke;

defaultgrid = 0;
// drawObject(defaultCube, 0, 1);

defaultScene = {
    objects: [
        // defaultCube,
        suzane,
        // blob,
    ],
    navTransforms: {
        orbitX: 0,
        orbitY: 70,
        orbitZ: 45,
        panX: 0,
        panY: 0,
        zoom: 1,
    },
    grid: defaultgrid,
}