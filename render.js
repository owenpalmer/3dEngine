drawgrid(100);

rot = rotatePoint(300, 300, 400, 500, 10);
drawpoint(rot[0], rot[1]);
// importedObj;
// translateObject(importedObj, 500, 500, 300);
// rotateAllPoints(importedObj, 501, 501, 10, 0, 2)

// for (time = 0; time < 500; time++) {
//     setTimeout(function () {
//         ctx.clearRect(0, 0, canvas.width, canvas.height);
//         rotateAllPoints(importedObj, 501, 501, .2, 0, 1);

//         drawObject(importedObj, 0, 2);

//     }, 60 * time)

// }

